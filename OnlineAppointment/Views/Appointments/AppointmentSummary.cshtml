@model IEnumerable<OnlineAppointment.Models.Appointment>

@{
    ViewBag.Title = "AppointmentSummary";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<br />
<h2>Reservation Summary</h2>
<h4> @ViewBag.State</h4>
<div class="col-lg-12" align="center">
    <br />
    <a href="~/Appointments/AppointmentSummary" class="btn btn-primary btn-xs">All Appointments</a>
    <a href="~/Appointments/Arrived" class="btn btn-primary btn-xs">Arrived</a>
    <a href="~/Appointments/Pending" class="btn btn-primary btn-xs">Pending</a>
    <a href="~/Appointments/Cancelled" class="btn btn-primary btn-xs">Cancelled</a>
    <a href="~/Appointments/NoShow" class="btn btn-primary btn-xs">No-Show</a>
</div>

<table class="table table-striped table-bordered col-md-12" id="example" align="center">
    <thead>
        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.AppointmentState.AppointmentStatus)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Product.ProductName)
                </th>*@

            <th>
                Customer Name
            </th>
            <th>
                Date
            </th>
            <th>
                Time
            </th>
            @*<th></th>*@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                        @Html.DisplayFor(modelItem => item.AppointmentState.AppointmentStatus)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Product.ProductName)
                    </td>*@

                <td>
                    @Html.DisplayFor(modelItem => item.User.FirstName)
                    @Html.DisplayFor(modelItem => item.User.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AppointmentDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Slot.Time)
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.AppointmentId }) |
                        @Html.ActionLink("Details", "Details", new { id=item.AppointmentId }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.AppointmentId })
                    </td>*@
            </tr>
        }

    </tbody>
</table>
