@model IEnumerable<OnlineAppointment.Models.Sale>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<br />
<h2>Sales Report</h2>
<h4> from @ViewBag.Start to @ViewBag.End</h4>
@*<div>
        <div>

            <span> start date:<input class="form-control input-sm" style="max-width:120px" type="text" id="startDate" placeholder="DD/MM/YYYY"></span>
            end date:
            <input class="form-control input-sm" style="max-width:120px" type="text" id="endDate" placeholder="DD/MM/YYYY">
            <input style="max-width:120px" type="submit" value="Search" class="btn btn-block btn-primary btn-xs    />
        </div>
        <br />
    </div>*@

@using (Html.BeginForm("Search", "Sales"))
{



@*<div class="row" style="flex-direction: row;">*@
    <div class="col-lg-12" align="center">
        <span>Start Date:</span>
        <input type="date" name="start" />
        <span>End Date:</span>
        <input type="date" name="end" /><br />
        <input style="margin-top:10px; max-width:120px" type="submit" value="Search" class="btn btn-block btn-primary btn-xs  />
        <br />
    </div>

    <br />
    <br />



        <div class="col-lg-12" align="center">
            <br />
            <a href="~/Sales/Index" class="btn btn-primary btn-xs">All Sales</a>
            <a href="~/Sales/Today" class="btn btn-primary btn-xs">Today</a>
            <a href="~/Sales/recentWeek" class="btn btn-primary btn-xs">Last 7 days</a>
            <a href="~/Sales/recentMonth" class="btn btn-primary btn-xs">Last 30 days</a>
            <a href="~/Sales/recentYear" class="btn btn-primary btn-xs">Last 365 days</a>
        </div>


        @*</div>*@
        @*<p>
            @Html.ActionLink("Create New", "Create")
        </p>*@
<div class="col-lg-12">
    <table class="table table-striped table-bordered" id="example">
        <thead>
            <tr>
                @*<th>
                    @Html.DisplayNameFor(model => model.DiscountType.DiscountTypeName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PaymentType.PaymentTypeName)
                </th>*@
                <th>
                    Name
                </th>
                <th style="text-align:center">
                    Sales No.
                </th>
                <th style="text-align:center">
                    Sales Date
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DiscountType.DiscountTypeName)
                </th>
                <th style="text-align: left; padding-left: 50px;">
                    Total
                </th>
                <th style="text-align: left; padding-left:50px;">
                    Discounted Total
                </th>
                @*<th>
                    @Html.DisplayNameFor(model => model.isPaid)
                </th>*@
                @*<th>Try Total</th>*@
            </tr>
        </thead>
  

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    @*<td>
                        @Html.DisplayFor(modelItem => item.DiscountType.DiscountTypeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PaymentType.PaymentTypeName)
                    </td>*@
                    <td>
                        @Html.DisplayFor(modelItem => item.User.FirstName)
                    </td>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.OrderNumber)
                    </td>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.OrderDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DiscountType.DiscountTypeName)
                    </td>
                    <td style="text-align:left; padding-left:50px;">
                        ₱ @Html.DisplayFor(modelItem => item.FinalTotal)
                    </td>
                    <td style="text-align: left; padding-left:50px;">
                        ₱ @Html.DisplayFor(modelItem => item.DiscountedTotal)
                    </td>
                    @*<td>
                        @Html.DisplayFor(modelItem => item.isPaid)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.SaleID }) |
                        @Html.ActionLink("Details", "Details", new { id=item.SaleID }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.SaleID })
                    </td>*@
                <!--<td>

                    @Model.Sum(modelItem => item.FinalTotal+ item.FinalTotal)
                    @Model.Sum(modelItem => item.DiscountedTotal)
                </td>-->
                </tr>
            }


        </tbody>

        <thead>
        <th>
            Total
        </th>
        <th style="text-align:center">
        </th>
        <th style="text-align:center">
        </th>
        <th style="text-align:center">
        </th>
        <th style="text-align: left; padding-left: 50px;">
            ₱@Model.Sum(i => i.FinalTotal)
        </th>
        <th style="text-align: left; padding-left:50px;">
            ₱@Model.Sum(i => i.DiscountedTotal)
        </th>
        </thead>

    </table>
    <div style="display: flex; flex-direction: column; align-items: flex-start; ">

        <strong>
            Date: @DateTime.Now

        </strong>
    </div>
</div>



       
       
 
}
</div>
            
