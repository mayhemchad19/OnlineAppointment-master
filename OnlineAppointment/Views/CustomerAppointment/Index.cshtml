@model IEnumerable<OnlineAppointment.Models.Appointment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}
<br /><br />
      <br />
<h2>My Appointments</h2>

<p>
    @Html.ActionLink("Set New Appointment", "Create")
</p>
<table id="example" class="table table-striped table-bordered">
    <thead>
        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.AppointmentState.AppointmentStatus)
                </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Product.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Slot.Time)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.User.Username)
                </th>*@
            <th>
                Date of Appointment
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                        @Html.DisplayFor(modelItem => item.AppointmentState.AppointmentStatus)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.Product.ProductName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Slot.Time)
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.User.Username)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.AppointmentDate)
                </td>
                <td style="text-align:center;">
                    @Html.ActionLink("Reschedule", "Edit", new { id = item.AppointmentId }) |
                    @*@Html.ActionLink("Details", "Details", new { id=item.AppointmentId }) |*@
                    @Html.ActionLink("Cancel Appointment", "Delete", new { id = item.AppointmentId } ,
                   new { onclick = "return confirm('Do you really want to Cancel this Appointment?')" })
                </td>
            </tr>
        }

    </tbody>

</table>
